<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ir.mkp.second_twitter">

    <application
        android:name=".utility.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity"/>
        <activity android:name=".activities.CreatePost"/>
        <activity android:name=".activities.LoginActivity"/>
        <activity android:name=".activities.RegisterActivity"/>
        <activity android:name=".activities.BioActivity"/>
        <activity android:name=".activities.CreateCommentActivity"/>
        <activity android:name=".activities.PostActivity"/>
        <activity android:name=".activities.CreateReplyActivity"/>
        <activity android:name=".activities.CommentActivity"/>
        <activity android:name=".activities.FollowerListActivity"/>
        <activity android:name=".activities.FollowingListActivity"/>
        <activity android:name=".activities.PostListActivity"/>
        <activity android:name=".activities.ForgetPassQuestion"/>
        <activity android:name=".activities.ChangePass"/>
    </application>

</manifest>